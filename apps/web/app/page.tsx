export default function UploadPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center gap-6 p-8 text-center">
      <h1 className="text-4xl font-bold">CloudTrim</h1>
      <p className="text-lg text-gray-600">
        Shift-left cloud cost optimizer — a reviewer that remediates.
      </p>

      {/* Upload UI (Terraform + billing CSV) is implemented in Week 1 MVP.
          See docs/BLUEPRINT.md §2. */}
      <div className="w-full rounded-lg border-2 border-dashed border-gray-300 p-12 text-gray-400">
        Upload page — coming in the Week 1 MVP.
      </div>
    </main>
  );
}
